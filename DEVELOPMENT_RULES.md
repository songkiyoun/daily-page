# DEVELOPMENT RULES

이 프로젝트는 기존 실험본처럼 구버전 위에 신버전 패치를 계속 덧붙이는 방식을 금지합니다.
모든 수정은 담당 파일의 기존 함수를 직접 수정하는 방식으로 진행합니다.

## 1. 프로젝트 방향 고정

- 1:1 전투 전용 게임입니다.
- 2:2, 3:3, 아군, 팀전, 다수 타겟 구조를 만들지 않습니다.
- 전장은 원형이 아니라 사각형입니다.
- 게임 흐름은 로그라이크 탑 등반 구조입니다.
- 상대의 무기, 성격, 스탯, 스킬은 매 층 랜덤으로 생성됩니다.
- 층이 오를수록 상대는 점점 강해집니다.
- 플레이어 성장은 경험치, 레벨, 스탯, 스킬, 무기 숙련을 기준으로 관리합니다.

## 2. 패치 누적 금지

금지 예시:

```javascript
const oldUpdate = updateBattle;
updateBattle = function patchedUpdateBattle() {
  oldUpdate();
  // 추가 패치
};
```

금지 예시:

```javascript
function resolveAttack() {}
function resolveAttack() {}
```

수정이 필요하면 기존 함수 본문을 직접 정리합니다.

## 3. 파일별 책임

- `data.js`: 무기, 성격, 스탯, 스킬, 보상, 층 성장 규칙
- `state.js`: 런 생성, 층 이동, 유닛 생성, 랜덤 상대 생성, 보상 적용
- `battle.js`: 공격 판정, 피해 계산, 승패 판정, 사각형 경계 처리
- `ai.js`: 1:1 이동 판단
- `render.js`: 캔버스 그리기
- `main.js`: UI 연결과 단일 게임 루프
- `utils.js`: 공통 수학 함수

## 4. 절대 중복되면 안 되는 함수

- `updateBattle`
- `resolveAttack`
- `decideMovement`
- `render`
- `createRun`
- `createBattleState`
- `applyRewardAndAdvance`
- `loop`

## 5. 수정 절차

1. 수정 목표를 한 문장으로 적습니다.
2. 수정할 파일을 정합니다.
3. 기존 함수를 직접 수정합니다.
4. 새 패치 블록을 추가하지 않습니다.
5. 실행 테스트를 합니다.
6. `CHANGELOG.md`에 변경 내용을 남깁니다.
7. GitHub에는 수정된 파일만 커밋합니다.

## 6. 커밋 메시지 예시

```text
v0.2.1: adjust square arena bounds
v0.2.2: improve random enemy floor scaling
v0.3.0: add stats skills and rewards
v0.3.1: fix reward button render lifecycle
```


## v0.4.0 전술 이동 기준

- 이동 판단은 `js/ai.js`의 `decideMovement`에서만 수정합니다.
- 공격 중 전진, 후딜 중 이탈, 몸 충돌 완화는 `js/battle.js`의 기존 함수에서 직접 수정합니다.
- 일자 접근을 고치기 위해 별도 패치 스크립트를 추가하지 않습니다.
- 무기별 이동 성향 수치는 `js/data.js`의 각 무기 데이터에서만 관리합니다.


## v0.4.1 이동 리팩터링 기준

- 무기별 이동 수치는 `data.js`의 `WEAPONS` 객체에서만 관리합니다.
- 성격별 이동 성향은 `data.js`의 `PERSONALITIES` 객체에서만 관리합니다.
- 이동 판단은 `ai.js`의 기존 `decideMovement`와 하위 이동 함수들을 직접 수정합니다.
- 단검 공격 각도 제한은 `battle.js`의 기존 `canStartAttack`에서만 관리합니다.
- 별도 `v0.4.1 patch` 스크립트나 중복 함수를 추가하지 않습니다.


## v0.4.2 성장 기본값 기준

- 시작 스탯 포인트는 `data.js`의 `PLAYER_START_STAT_POINTS`에서만 관리합니다.
- 층 이동 시 완전 회복은 `state.js`의 `applyRewardAndAdvance` 흐름에서만 처리합니다.
- 보상 선택 이후 새 층 전투 시작 흐름은 `main.js`의 기존 보상 선택 핸들러를 직접 수정해 관리합니다.
- 회복 보상처럼 현재 규칙과 충돌하는 보상은 새 패치로 덮지 않고 기존 보상 생성 로직에서 직접 교체합니다.


## v0.4.3 전투 리듬 기준

- 무기별 회전 속도와 자세 피해량은 `data.js`의 `WEAPONS`와 `POSTURE_RULES`에서만 관리합니다.
- 방향 전환 제한, 공격 후딜, 자세 흐트러짐은 `battle.js`의 기존 `updateUnit`, `getTurnSpeed`, `resolveAttack` 흐름에서만 처리합니다.
- 자세 게이지 생성은 `state.js`의 유닛 생성 구조에서만 처리합니다.
- 자세 게이지 표시는 `render.js`와 `main.js`의 기존 표시 흐름에서 직접 관리합니다.
- 단검이 자세 흐트러짐을 이용하는 움직임은 `ai.js`의 기존 이동 판단 함수 안에서 처리합니다.
- 패링은 아직 별도 단계이며, 이 버전에서는 자세 흐트러짐과 회전 제한만 다룹니다.
- 별도 `v0.4.3 patch` 스크립트나 중복 함수를 추가하지 않습니다.



## v0.4.4 UI 구조 기준

- 캐릭터 세팅, 스탯 설정, 추후 상점 영역은 왼쪽 패널에서 관리합니다.
- 전투, 승리·패배 결과, 보상 선택은 중앙 전투 오버레이에서 처리합니다.
- 탑 정보와 상태 정보는 오른쪽 패널에서만 표시합니다.
- 클릭 가능한 UI는 매 프레임 새로 만들지 않고, 상태 변화가 있을 때만 기존 렌더링 함수를 통해 갱신합니다.
- 스탯 클릭은 `main.js`의 기존 이벤트 위임 흐름에서만 처리합니다.
- 보상 선택은 `main.js`의 중앙 오버레이 보상 처리 흐름에서만 관리합니다.
- 별도 `v0.4.4 patch` 스크립트나 중복 함수를 추가하지 않습니다.


## v0.4.6 후퇴 제한·자세 리듬 기준

- 후퇴 제한 수치는 `data.js`의 `POSTURE_RULES`에서만 관리합니다.
- 방어형과 창의 무한 후퇴 방지는 `ai.js`의 기존 후퇴·창 이동 함수에서만 처리합니다.
- 후퇴 프레임, 후퇴 제한, 근접 밀어내기, 무기 충돌은 `battle.js`의 기존 전투 흐름에서 직접 관리합니다.
- 공격 성공과 무기 충돌로 몸 방향이 틀어지는 처리는 `battle.js`의 기존 피격·자세 흐름에 통합합니다.
- 자세가 0일 때의 스태미너 붕괴는 기존 `staggerTimer` 구조를 사용하며, 별도 중복 상태머신을 만들지 않습니다.
- 별도 `v0.4.6 patch` 스크립트나 중복 함수를 추가하지 않습니다.


## v0.4.8 안정화 기준

- 중앙 전투 버튼은 기존 런을 시작해야 하며, 새 런을 다시 생성해 스탯을 초기화하면 안 됩니다.
- 밀치기와 무기 충돌은 실제 근접 또는 실제 타격 판정 상태에서만 발생해야 합니다.
- 창·방어형은 후퇴만 반복하지 않고, 일정 거리 이후 측면 견제와 짧은 재진입으로 전환해야 합니다.
- 단검 미러전은 무한 궤도 이동이 아니라 짧은 교전, 이탈, 재진입으로 흘러야 합니다.
- 위 수정도 새 패치 블록 없이 기존 함수 본문을 직접 수정합니다.


## v0.4.9 추가 기준

- 패링, 자세 피해, 무기 충돌, 스태미너 붕괴는 `battle.js`의 기존 전투 흐름 안에서만 수정합니다.
- 패링 관련 수치와 무기별 패링 효율은 `data.js`에서만 관리합니다.
- 패링 시각 효과는 `render.js`에서만 처리하며 전투 계산에 영향을 주지 않습니다.

## v0.5.1 추가 기준

- 무기별 공격 리듬 수치는 `data.js`의 WEAPONS 안에서만 관리합니다.
- 공격 준비, 타격, 후딜 전환은 `battle.js`의 기존 `updateAttackState` 흐름 안에서만 수정합니다.
- 타격 궤적과 무기 시각화는 `render.js`에서만 처리하며 전투 계산에 영향을 주지 않습니다.
- `impactStopTimer`는 타격 순간을 보여주기 위한 짧은 전투 리듬 장치이며, 별도 패치 함수로 덧붙이지 않습니다.



## v0.5.4 명중·패링·충돌 반응 기준

- 명중 넉백은 `data.js`의 각 무기 `hitKnockback`, `hitPostureScale`, `selfRetreatOnHit` 수치에서 관리합니다.
- 패링 시 밀림은 `data.js`의 `parryKnockbackTaken`과 `POSTURE_RULES.parryKnockback`에서만 관리합니다.
- 무기 충돌 시 밀림은 `data.js`의 `clashKnockbackScale`과 `POSTURE_RULES.weaponClashKnockback`에서만 관리합니다.
- 명중 품질 계산과 실제 반응은 `battle.js`의 기존 공격 판정 흐름 안에서 처리합니다.
- 별도 패치 함수나 중복 `resolveAttack`을 추가하지 않습니다.


## v0.5.6 이벤트 표시·단검 페이크 기준

- 전투 이벤트 텍스트는 `render.js`의 캔버스 렌더링에서만 표시합니다.
- 전투 중 DOM 요소를 새로 만들어 HIT, MISS 텍스트를 띄우지 않습니다.
- 일반 명중과 일반 빗나감은 텍스트로 표시하지 않습니다.
- 텍스트 표시는 PARRY, CLASH, BREAK, CRITICAL처럼 전투 흐름이 바뀌는 핵심 이벤트만 허용합니다.
- 피격 넉백은 `battle.js`의 기존 impact stop 흐름 안에서 직접 관리합니다.
- 단검 페이크, 반대 꺾기, 순간 침투는 `ai.js`의 단검 이동 판단 함수 안에서만 관리합니다.
- 단검 명중 후 재정렬 시간은 유닛 상태와 `POSTURE_RULES`에서 관리하며 별도 패치 함수로 덮지 않습니다.
- 별도 `v0.5.6 patch` 스크립트나 중복 함수를 추가하지 않습니다.

## v0.5.7 수정 원칙

- 창 거리 재설정은 `data.js`, `ai.js`, `battle.js`의 기존 수치와 기존 함수에서 직접 조정합니다.
- 단검 페이크는 `data.js`의 무기 정체성 수치와 `ai.js`의 기존 `daggerFeintMovement` 흐름에서 직접 조정합니다.
- 별도 `v0.5.7 patch` 함수, 중복 `decideMovement`, 중복 `resolveAttack` 선언을 만들지 않습니다.
- 이번 버전 확인 기준은 창 명중 후 거리 재설정 체감, 단검 좌우 흔들림의 시각적 체감, 단검이 정면 재진입으로 돌아가는 빈도입니다.

## v0.5.9 단검 빈틈 진입·페이크 수정 기준

- 단검 빈틈 진입은 `ai.js`의 기존 `daggerFeintMovement` 안에서만 관리합니다.
- 단검 페이크 수치, 반대 꺾기 시간, 상대 정자세 회복 지연은 `data.js`의 기존 `WEAPONS.dagger`와 `POSTURE_RULES`에서만 관리합니다.
- 상대가 빈틈을 보였을 때 즉시 침투하는 조건은 새 패치 함수가 아니라 기존 단검 이동 흐름 안에서 처리합니다.
- 별도 `v0.5.9 patch` 스크립트나 중복 `decideMovement` 선언을 만들지 않습니다.


## v0.6.2 회전·회복·복귀 분리 기준

- 평소 회전, 흔들림 회복, 공격 후 원자세 복귀는 같은 값으로 처리하지 않습니다.
- 평소 회전 순서: 서양검 < 창 < 동양검 < 단검.
- 흔들림 회복 순서: 창 < 서양검 < 동양검 < 단검.
- 공격 후 원자세 복귀 순서: 창 < 서양검 < 동양검 < 단검.
- 창은 명중 성공 시 거리 재설정 보상을 유지하되, 빗나감·패링·흔들림 상황에서는 가장 큰 빈틈을 갖습니다.
- 이 기준을 바꿀 때는 `data.js`의 무기 데이터와 `battle.js`의 기존 회전·회복 함수만 직접 수정합니다.

## v0.6.0 무기별 속도 체계 기준

- 무기별 기본 이동속도, 방향전환, 공격 쿨타임은 `data.js`의 `WEAPONS`에서만 관리합니다.
- 민첩 보정은 `state.js`의 기존 `getWeaponAgilityScales`에서만 계산합니다.
- 전투 중 실제 회전 적용은 `battle.js`의 기존 `getTurnSpeed`에서만 처리합니다.
- 단검 미러전 이동 규칙은 `ai.js`의 기존 `daggerMirrorMovement`에서 직접 관리합니다.
- 민첩이 높아도 서양검이 단검처럼 돌거나, 창이 단검처럼 움직이면 안 됩니다.
- 별도 `v0.6.0 patch` 함수, 중복 `decideMovement`, 중복 `getTurnSpeed`, 중복 `derivePlayerProfile` 선언을 만들지 않습니다.


## v0.6.3 성격 시스템 재정의 기준

- 성격은 무기 정체성을 덮지 않고 전투 수치와 운용 성향을 보정합니다.
- 공격형은 공격력, 공격속도, 밀쳐내기, 자세 압박을 올리고 방어력과 패링 확률을 낮춥니다.
- 방어형은 방어력, 패링, 밀림 저항, 자세 안정성을 올리고 공격력과 공격속도를 낮춥니다.
- 밸런스형은 기준값 역할을 하므로 과한 보정을 넣지 않습니다.
- 암살형은 이동, 회전, 측후방, 치명타를 올리고 방어력과 밀림 저항을 낮춥니다.
- 성격 수치 변경은 `data.js`의 `PERSONALITIES`에서만 관리합니다.
- 파생 능력치 반영은 `state.js`의 기존 `derivePlayerProfile`, `deriveEnemyProfile`에서만 처리합니다.
- 넉백·자세 피해 반영은 `battle.js`의 기존 피격·패링·충돌 함수에서만 처리합니다.
- 별도 `v0.6.3 patch` 함수, 중복 `derivePlayerProfile`, 중복 `resolveAttack`, 중복 `getTurnSpeed`를 만들지 않습니다.

## v0.6.9 1층 기준 스탯·시뮬레이터 기준

- 1층 밸런스 테스트의 기준 상대 스탯은 힘·체력·방어·민첩·행운 모두 5입니다.
- 1층 상대는 숙련도 0, 스킬 없음 상태를 기준으로 합니다.
- 상대 스탯 생성식은 `state.js`의 기존 적 생성 흐름에서 직접 관리합니다.
- 시뮬레이터는 별도 전투 로직을 만들지 않고 기존 `updateBattle`을 그대로 사용합니다.
- 시뮬레이터용 고정 상대는 `createFixedEnemyConfig`로만 생성합니다.
- 시뮬레이터 결과는 승률, 평균 명중, 평균 시간, 잔여 체력을 중심으로 봅니다.
- 별도 `simulationBattle` 전투 판정 함수나 중복 `resolveAttack`, 중복 `updateBattle`을 만들지 않습니다.


## v0.6.9 추가 UI 원칙
- 전투 검수 도구는 전투 화면과 가까운 중앙 영역에 둡니다.
- 시뮬레이터는 세팅 패널 아래로 밀어 넣지 않습니다.
- 결과는 복사 가능한 텍스트 형태로 함께 제공합니다.


## v0.6.9 시뮬레이터 수정 원칙

- 시뮬레이터도 실제 전투 로직을 사용합니다.
- 시뮬레이터 공정성 검증은 A/B 양방향 결과를 평균내는 방식으로 처리합니다.
- 미러전 결과가 과도하게 한쪽으로 치우치면 무기 밸런스보다 시뮬레이터 편향을 먼저 점검합니다.
- 시뮬레이터 기능도 새 패치 블록을 덧붙이지 않고 기존 함수 구조를 직접 수정합니다.


## v0.6.9 밸런스 조정 원칙

- 이번 조정은 최종 밸런스가 아니라 기초 체급 보정입니다.
- 모든 조합을 50:50으로 맞추지 않습니다.
- 동양검은 재미를 유지하되 과한 압박만 낮춥니다.
- 서양검은 데미지보다 정면 가드, 패링 반격, 자세 붕괴로 살립니다.
- 방어형은 오래 버티되 공격 결정력은 낮아야 합니다.


## v0.6.9 밸런스 조정 원칙

- 단검은 완전 후방 암살보다 측면·측후면 침투 중심으로 운용합니다.
- 서양검은 범위를 무작정 넓히지 않고, 단검 근접 상황에서만 견제 수단을 갖습니다.
- 동양검은 맞은 뒤 패링이 아니라 맞기 직전 흘리기로 처리합니다.
- 이번 조정도 최종 밸런스가 아니라 스킬 시스템 전 기초 체급 보정입니다.


## v0.6.9 밸런스 조정 원칙

- 단검은 동양검을 정면에서 이기는 무기가 아니라, 연속공격 사이를 찌르고 빠져나오는 무기입니다.
- 대동양검 보정은 확률·조건부로만 적용합니다.
- 단검의 측면 즉시 찌르기는 유지하되, 서양검 상대로는 진입 속도를 낮춰 근접 견제가 작동할 여지를 남깁니다.
- 스킬 시스템 전 마지막 기초 밸런스 조정으로 보고, 이후 큰 수치 조정은 스킬 구조가 들어간 뒤 판단합니다.


## v0.7.13 단검 기본 공격 원칙

- 단검은 완전 후방보다 측면 공격 각을 우선합니다.
- 단검이 측면 90도 근처에 있고 공격 가능 거리라면 후퇴보다 찌르기 진입을 우선합니다.
- 공격 쿨타임이 조금 남아 있는 경우에는 측면에서 공격 대기하며 각을 유지합니다.
- 단검은 몸이 완전히 겹쳤을 때만 짧게 이탈합니다.
- 창과 서양검은 별도 대응 로직을 유지해 단검이 전체적으로 과하게 강해지지 않도록 합니다.


## v0.7.13 단검 AI 수정 원칙

- 단검이 측면 각도를 잡았을 때는 후퇴·재정렬보다 공격 의지를 우선합니다.
- 단검의 후퇴는 몸이 완전히 겹친 경우에만 짧게 허용합니다.
- 단검 공격 방식 보정은 수치 버프가 아니라 AI 우선순위 수정으로 처리합니다.
- 이펙트, 히트스톱, 타격 연출은 이후 별도 단계에서 처리합니다.


## v0.7.13 조정 원칙

- 단검의 정체성은 긴 후퇴가 아니라 짧은 사선 이탈과 측면 재진입입니다.
- 단검 회피 반격은 전체 상시 발동이 아니라 창의 정면 찌르기 위협에 한정합니다.
- 창 방어형은 버티는 무기이지만, 비타격 자세 피해로 상대를 과하게 무력화하지 않도록 합니다.
- 단검과 창의 상성은 창이 기본적으로 유리하되, 단검 암살형은 비빌 수 있는 정도를 목표로 합니다.


## v0.7.13 조정 원칙

- 단검은 멀리 도망가지 않고 가까운 측면을 유지하되, 바로 연속 공격하지 않도록 공격 후 쿨타임을 둡니다.
- 단검이 창 안쪽에 들어오면 공격 취소를 줄이지만, 창을 상대로 측면 보너스는 낮게 적용합니다.
- 창의 비타격 자세 피해는 유지하되 단검에게 과도하게 들어가지 않도록 제한합니다.
- 단검의 전투감은 움직임으로 살리고, 대창 밸런스는 피해·자세 보너스로 제어합니다.


## v0.7.13 조정 원칙

- 단검은 계속 도는 무기가 아니라, 정면 바깥에서 유인하고 사선으로 빠진 뒤 측면을 찌르는 무기입니다.
- 단검의 측면 진입은 상시 원형 회전이 아니라 적 공격, 후딜, 자세 회복 타이밍에 강해져야 합니다.
- 정면 찌르기는 마지막 보험으로 유지합니다.
- 이번 버전은 단검 이동 구조 개편 테스트가 핵심이며, 전체 밸런스는 50회 양방향 시뮬과 실제 전투 체감으로 다시 판단합니다.


## v0.7.13 조정 원칙

- 정면에서는 유인, 측면에서는 공격 준비, 후면에서는 즉시 진입을 우선합니다.
- 단검이 창을 더 자주 찌르게 된 만큼, 대창 피해 보상은 낮게 유지합니다.
- 이번 버전은 단검의 동작 체감 확인이 중요하며, 전체 밸런스는 50회 양방향 시뮬과 실제 전투 테스트로 다시 판단합니다.


## v0.7.13 마감 원칙

- v0.7.13은 기초 전투 밸런스 마감판입니다.
- 단검의 대창 피해 보정 외에는 전투 구조를 변경하지 않습니다.
- 이후 밸런스 변화는 v0.7.13 스킬 시스템, 무기 진화, 고유 능력 추가 이후 다시 판단합니다.


## v0.7.13 개발 원칙

- v0.7.13은 스킬 완성판이 아니라 스킬 시스템 뼈대 버전입니다.
- 기본 스킬은 무기와 성격 정체성을 보여주는 약한 자동 발동 능력으로 시작합니다.
- 스킬 밸런스는 v0.7.13 이후 실제 전투와 50회 양방향 시뮬레이션으로 조정합니다.
- v0.7.13에서는 스킬 구조, 보상 구조, 레벨 구조가 정상 작동하는지 우선 확인합니다.


## v0.7.13 조정 원칙

- 스킬 효과보다 스킬 발동 확인과 모션 연결을 우선합니다.
- 글자만 뜨는 스킬은 실제 공격 상태나 이동 변화와 연결합니다.
- 방어형 반사처럼 전투를 답답하게 만드는 자동 발동은 긴 쿨타임과 강한 조건을 둡니다.
- v0.7.13 이후에는 각 스킬의 체감 강도를 실제 전투 기준으로 다시 조정합니다.


## v0.7.13 조정 원칙

- 창의 `집중`은 방어적 반응입니다.
- 창의 `벤다!`는 측후면 공격을 받았을 때 나오는 반격 베기입니다.
- `벤다!`는 자주 나오는 스킬이 아니라, 창의 등 뒤를 잡은 적을 밀어내는 장면을 만들기 위한 스킬입니다.
- 스킬 발동 문구가 보이는 것과 실제 발동 횟수는 다를 수 있습니다. 문구는 여러 프레임 동안 유지됩니다.


## v0.7.13 조정 원칙

- 창의 `벤다!`는 측후면 공격을 받았을 때 나오는 반격기입니다.
- 창의 `집중`은 자세가 무너졌을 때 창끝을 다시 상대에게 맞추는 안정화 스킬입니다.
- 두 스킬이 같은 조건에서 경쟁하지 않도록 분리합니다.
- `집중`은 피해를 주는 스킬이 아니라 자세 회복, 방향 보정, 짧은 준비시간 보정을 담당합니다.


## v0.7.13 검수 원칙

- 플레이어와 적은 같은 기준으로 무기 스킬 3개와 성격 스킬 3개를 가져야 합니다.
- 실제 탑 랜덤 적, 고정 시뮬 적, 플레이어 생성 구조 모두 스킬 레벨 정보를 함께 가져야 합니다.
- 스킬 밸런스 검토는 v0.7.13 이후부터 실제 조건이 맞춰진 상태에서 진행합니다.


## v0.7.13 조정 원칙

- 지금 단계는 1층 / 스탯 5 / 스킬 Lv.1 기준입니다.
- 완전한 50:50 밸런스를 목표로 하지 않습니다.
- 단검 방어형과 창 방어형처럼 초반부터 과하게 튀는 조합만 소폭 보정합니다.
- 암살형은 후반 스탯, 스킬 레벨업, 무기 진화 이후 다시 판단합니다.


## v0.7.13 조정 원칙

- 무기 등급 상승과 무기 진화는 아직 실제 보상으로 넣지 않습니다.
- 전설 보상은 구조상 자리만 남기고, 실제 효과는 v0.8.0 이후에 적용합니다.
- 보상은 현재 탑 진행 안에서만 유지되는 성장입니다.
- 일반은 기본 성장, 희귀는 장단점, 영웅은 큰 성장, 전설은 추후 무기 진화와 특수 보상으로 사용합니다.
- 스킬 성장폭과 보상 체감은 v0.7.13에서 실제 진행 기준으로 다시 검수합니다.


## v0.7.13 문구 원칙

- 보상 제목은 짧고 즉시 이해되게 작성합니다.
- 보상 설명에는 실제 효과와 수치를 우선 표시합니다.
- 아직 구현되지 않은 예정 기능 안내 문구는 보상 설명에서 제거합니다.
- 스킬 습득 보상은 `스킬명 습득` 형태로 표시합니다.


## v0.7.13 문구 원칙

- 보상 등급은 카드 상단에만 표시합니다.
- 보상 제목에는 등급을 중복으로 넣지 않습니다.
- 보상 설명은 효과와 수치 중심으로 짧게 표시합니다.
- 불필요한 설명 문장과 온점은 제거합니다.


## v0.7.13 조정 원칙

- 창의 `벤다!`는 정식 반격기입니다.
- 일반 `창 밀어내기`는 단검의 모든 공격 성공마다 발동하면 안 됩니다.
- 단검이 명중한 직후에는 창의 일반 근접 밀어내기를 잠시 제한합니다.
- 단검이 공격 중일 때는 일반 밀어내기보다 공격 결과와 반격 스킬을 우선합니다.


## v0.7.13 조정 원칙

- 단검이 명중한 직후에는 일반 근접 견제를 잠시 제한합니다.
- 서양검의 정식 반응 스킬인 `기사의 직감`은 유지합니다.
- 일반 근접 견제가 단검의 공격 성공감을 매번 지우지 않도록 조정합니다.
- 실제 서양검 명중, 패링, 정식 스킬로 인한 자세 손실은 유지합니다.


## v0.7.13 이펙트 원칙

- 이펙트는 밸런스 수치를 변경하지 않습니다.
- 전투 판정과 이펙트는 분리합니다.
- 과한 연출보다 명중, 치명타, 패링, 자세 붕괴, 넉백을 읽기 쉽게 만드는 것을 우선합니다.
- 화면 흔들림은 짧고 약하게 적용합니다.
- 이펙트가 전투 가독성을 해치면 v0.7.13에서 강도를 줄입니다.


## v0.7.13 이펙트 원칙

- 밸런스 수치는 건드리지 않고 타격 체감만 강화합니다.
- 일반 명중도 맞았다는 순간이 확실히 보여야 합니다.
- 치명타와 자세 붕괴는 일반 명중보다 더 강하게 보여야 합니다.
- 화면 흔들림은 타격감 보조용이며, 불편하면 다음 버전에서 낮춥니다.


## v0.7.13 이펙트 원칙

- 이펙트 강도보다 무기별 형태 차이를 우선합니다.
- 서양검은 넓고 묵직하게, 동양검은 빠르게, 창은 직선적으로, 단검은 짧고 날카롭게 표현합니다.
- 밸런스 수치는 변경하지 않습니다.
- 강한 연출 완성판은 무기 진화와 고유 능력 이후에 적용합니다.
